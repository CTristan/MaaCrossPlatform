using System.Collections.Generic;

namespace MaaGui.Helper;

public static class ItemListHelper
{
    public static string? GetItemName(string itemId)
    {
        // Simple stub for now, in a real app we'd load item_index.json
        return itemId;
    }
}
